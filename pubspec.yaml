name: nyobahp
description: "A new Flutter project."
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter
  tflite_flutter: any
  image: any
  image_picker: any
  cupertino_icons: any
  google_fonts: any

dependency_overrides:
  image: ^3.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/sawit.tflite
    - assets/ipalms_logo.png